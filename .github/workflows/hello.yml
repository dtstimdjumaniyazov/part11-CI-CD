name: Ex-3-4

on:
  push:
    branches: [main, master]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      - name: Run date
        run: |
          TZ='Asia/Tashkent' date
      - name: Run ls -l
        run: |
          ls -l